const route=new URLSearchParams(location.search).get("route")||"EASTON-001";const ENDPOINT="";function event(name,data={}){const payload={event:name,route,ts:new Date().toISOString(),path:location.pathname,...data};console.log("[EASTON]",payload);try{const events=JSON.parse(localStorage.getItem("easton_events")||"[]");events.push(payload);localStorage.setItem("easton_events",JSON.stringify(events))}catch{}if(ENDPOINT)fetch(ENDPOINT,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(payload),keepalive:true}).catch(()=>{})}event("visit");const btn=document.querySelector("#interestBtn"),formCard=document.querySelector("#formCard"),form=document.querySelector("#leadForm"),thanks=document.querySelector("#thanks");btn.addEventListener("click",()=>{event("cta_click");formCard.classList.remove("hidden");formCard.scrollIntoView({behavior:"smooth"})});form.addEventListener("submit",async e=>{e.preventDefault();const data=Object.fromEntries(new FormData(form));event("lead_submit",{binCount:data.binCount,priceAcceptance:data.priceAcceptance});if(ENDPOINT){try{await fetch(ENDPOINT,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({event:"lead",route,ts:new Date().toISOString(),...data})})}catch{}}formCard.classList.add("hidden");thanks.classList.remove("hidden");thanks.scrollIntoView({behavior:"smooth"});});